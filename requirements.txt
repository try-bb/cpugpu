# GPU 监控 + 风扇控制 - 依赖列表
# 安装: pip install -r requirements.txt

# 核心依赖
PyQt5>=5.15.0
psutil>=5.9.0
requests>=2.28.0

# 硬件监控（LibreHardwareMonitor 统一读取 CPU/GPU 信息）
pythonnet>=3.0.0
PyLibreHardwareMonitor>=1.2.0

# 麦克风音量锁定（可选，Windows only）
pycaw>=20230407; sys_platform == "win32"
comtypes>=1.1.0; sys_platform == "win32"
